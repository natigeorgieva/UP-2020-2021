for (int j = M - 1; j >= 0; j--)//започваме от последния стълб и вървим наляво
{
	for (int i = M - 1; i+ j >= M - 1; i--)//започваме от последния ред и вървим нагоре
	{
		cout << arr[i][j] << " ";
	}
	int copy = j - 1;//не искаме да променяме номера на стълба в брояча на фор цикъла и затова си правим копие, което е с едно наляво, защото вече сме изпринтили елемента на диагонала
	while (copy >= 0)
	{
		cout << arr[M - 1- j][copy] << " ";
		copy--;
	}
	cout << endl;
}
